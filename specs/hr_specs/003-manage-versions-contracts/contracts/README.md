# Contracts

This folder should contain API and UX contract files if external integrations are exposed (e.g., REST/GraphQL endpoints or import/export CSV schemas).

For now: no external contract defined. Add contract specs here when needed.
